# poker equity calculator in typescript

https://www.npmjs.com/package/poker-equity

not the fastest not the most accurate but does its job


```
$ node dist/main.js --help
Usage: Poker Equity CLI [options]

A powerful CLI tool to calculate poker equities in typescript

Options:
  -V, --version        output the version number
  -h, --hands <hands>  Specify hands
  -b, --board <board>  Specify board cards
  --help               display help for command
```

download directly the release and not compile this shit
```
wget https://github.com/sbOogway/poker-equity-ts/releases/download/prod/main.js
```